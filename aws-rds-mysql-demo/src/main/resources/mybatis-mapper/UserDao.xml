<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace:改mapper.xml映射文件的唯一标识并且必须和数据处理层的接口的路径相同-->
<mapper namespace="com.example.awsrdsmysqldemo.daos.UserDao">
    <!--   必须添加property属性 ，区别于是否加主键-->
    <resultMap id="baseMap" type="User">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="mail" property="mail" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
    </resultMap>

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into user(username, mail, age)
        values (#{username}, #{mail}, #{age})
    </insert>

    <select id="queryUserByName" resultMap="baseMap">
        select id,
               username,
               mail,
               age
        from user
        where username = #{username}
    </select>
</mapper>